# Metadata for the templates-v* release manifest. Adding a template means adding a directory
# under src/ with a Dockerfile and one of these, plus an .abcversion.json project keyed by the
# `short` name below — that project is what scopes the template's version to its own commits.
name: sbx-claude-python-uv
short: python-uv
description: Latest Python via uv
