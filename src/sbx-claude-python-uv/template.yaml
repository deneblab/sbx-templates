# Metadata for the templates-v* release manifest. Adding a template means adding a
# directory under src/ with a Dockerfile and one of these — nothing else to update.
name: sbx-claude-python-uv
short: python-uv
description: Latest Python via uv
